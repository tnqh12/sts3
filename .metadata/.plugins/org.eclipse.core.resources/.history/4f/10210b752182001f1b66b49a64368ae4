/* 전체적인 레이아웃을 위한 기본 스타일 */
body {
    font-family: 'Cinzel', serif; /* 중세 느낌의 폰트 */
    font-weight: bold; /* 폰트를 굵게 설정 */
    background-color: #1a1a2e; /* 어두운 밤 하늘 색상 */
    color: #eaeaea; /* 텍스트를 밝게 */
    margin: 0;
}

.container {
    width: 90%;
    margin: 0 auto;
}

/* 헤더 스타일 */
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 0;
    background-color: #16213e; /* 헤더 배경을 어둡게 */
    border-bottom: 2px solid #0f3460;
}

.logo {
    height: 100px;
    cursor: pointer;
    margin-left: 0;
}

/* 로그인 및 로그아웃 박스 */
.login-box {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    flex-grow: 1;
    margin-right: 130px;
}

.login-box span {
    margin-right: 10px;
    font-size: 16px;
    font-weight: bold; /* 폰트를 굵게 설정 */
}

.login-button {
    padding: 8px 16px;
    margin-left: 10px;
    border: 1px solid #e94560;
    background-color: #0f3460;
    color: #eaeaea;
    cursor: pointer;
    border-radius: 5px;
    transition: background-color 0.3s;
    font-weight: bold; /* 폰트를 굵게 설정 */
}

.login-button:hover {
    background-color: #e94560;
    border-color: #e94560;
}

/* 스토리 목록 스타일 */
.story-list {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    justify-items: center;
    padding: 40px 0;
}

.story-item {
    width: 100%;
    max-width: 300px;
    background-color: #16213e;
    border: 1px solid #0f3460;
    border-radius: 10px;
    overflow: hidden;
    text-align: center;
    box-shadow: 0 4px 8px rgba(14, 14, 29, 0.7);
    cursor: pointer;
    transition: transform 0.3s;
}

.story-item:hover {
    transform: translateY(-10px);
}

.story-item img {
    width: 100%;
    height: auto;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    display: block;
}

/* 제목과 버튼 스타일 */
.story-item .card-body {
    padding: 20px;
}

.story-item .card-title {
    font-size: 20px;
    font-weight: bold; /* 이미 굵게 설정됨 */
    margin: 15px 0;
    color: #e94560;
}

.story-item .card-text {
    font-size: 16px;
    color: #a6a6a6;
    margin-bottom: 20px;
    font-weight: bold; /* 폰트를 굵게 설정 */
}

.story-item button {
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    background-color: #e94560;
    color: #eaeaea;
    cursor: pointer;
    transition: background-color 0.3s;
    font-weight: bold; /* 폰트를 굵게 설정 */
}

.story-item button:hover {
    background-color: #0f3460;
}

/* 추가 스타일 */
h1 {
    text-align: center;
    margin-top: 30px;
    color: #e94560;
    font-weight: bold; /* 폰트를 굵게 설정 */
}

/* 모달 스타일 */
.modal-content {
    background-color: #1a1a2e;
    padding: 20px;
    border-radius: 5px;
    box-shadow: 0 2px 10px rgba(14, 14, 29, 0.7);
    color: #eaeaea;
    font-weight: bold; /* 폰트를 굵게 설정 */
}

.modal-header, .modal-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.modal-body img {
    max-width: 100%;
    height: auto;
    margin-bottom: 20px;
}

/* 링크 스타일 */
a {
    color: #e94560;
    text-decoration: none;
    font-weight: bold; /* 폰트를 굵게 설정 */
}

a:hover {
    text-decoration: underline;
}

/* 폰트 임포트 */
@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@700&display=swap'); /* 굵은 폰트 로드 */
