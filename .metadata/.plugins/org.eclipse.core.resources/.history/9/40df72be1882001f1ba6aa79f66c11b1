/* 애니메이션 효과를 위한 키프레임 */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
/* 배경 흐림 효과 */
body {
  background-image: url('/StoryCraft/resources/img/tree.png');
  background-size: cover;
  background-position: center;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  animation: fadeIn 1s ease-in-out;
  position: relative;
}

/* 로고 스타일 및 애니메이션 */
.logo-container {
  position: absolute;
  top: 20px;
  left: 20px;
}

.small-logo {
  width: 40px;
  height: auto;
  animation: logoSpin 5s infinite linear; /* 로고 스핀 애니메이션 */
}

.logo-animation {
  transition: transform 0.5s;
}

.logo-animation:hover {
  transform: scale(1.2); /* 호버 시 크기 확대 */
}

/* 컨테이너 */
.container {
  width: 100%;
  max-width: 400px;
  background-color: rgba(0, 0, 0, 0.5); /* 더 낮은 투명도 */
  padding: 30px;
  border-radius: 10px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
  animation: fadeIn 1s ease-out;
}
/* 레이블 텍스트 색상을 흰색으로 설정 */
label {
  color: #ffffff; 
}


/* 제목 */
h2 {
  text-align: center;
  margin-bottom: 20px;
  color: #f5f5f5; /* 더 진한 색상 */
  font-size: 24px;
  font-weight: bold;
}

/* 인풋 필드 스타일 */
.form-control {
  width: 100%;
  padding: 15px;
  margin-bottom: 20px;
  font-size: 16px;
  border: 1px solid #ddd;
  border-radius: 5px;
  transition: border-color 0.3s, box-shadow 0.3s;
  box-sizing: border-box;
  color: #ffffff; /* 글자 색상을 흰색으로 변경 */
  background-color: rgba(255, 255, 255, 0.1); /* 인풋 필드 배경 투명도 */
}

.form-control:focus {
  border-color: #007bff;
  box-shadow: 0 0 10px rgba(0, 123, 255, 0.2);
  outline: none;
}

/* 버튼 스타일 */
.btn {
  display: block;
  width: 100%;
  padding: 15px;
  background-color: #007bff;
  color: white;
  font-size: 18px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s, transform 0.2s, box-shadow 0.3s;
}

.btn:hover {
  background-color: #0056b3;
  transform: scale(1.05);
  box-shadow: 0 4px 15px rgba(0, 86, 179, 0.4);
}

/* 인풋 그룹 */
.input-group {
  position: relative;
}

.input-icon {
  position: absolute;
  right: 10px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 18px;
  color: #007bff;
}

/* 버튼 애니메이션 */
.btn-animation {
  transition: background-color 0.3s, transform 0.2s, box-shadow 0.3s;
}

.btn-animation:hover {
  background-color: #0056b3;
  transform: scale(1.05);
  box-shadow: 0 4px 15px rgba(0, 86, 179, 0.4);
}

/* 모달 애니메이션 */
.modal-animation {
  animation: fadeIn 0.5s ease-in-out;
}

/* 모달 기본 */
/* 모달 배경 */
.modal {
    display: none; /* 기본적으로 숨김 */
    position: fixed;
    z-index: 1000; /* 다른 요소들보다 위에 표시 */
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto; /* 내용이 넘칠 경우 스크롤 */
    background-color: rgba(0, 0, 0, 0.5); /* 반투명 검정 배경 */
}

/* 모달 내용 */
.modal-content {
    background-color: #ffffff;
    margin: 10% auto; /* 수직 및 수평 중앙 정렬 */
    padding: 20px;
    border: 1px solid #888;
    width: 80%; /* 모달의 너비 */
    max-width: 500px; /* 최대 너비 */
    border-radius: 8px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    text-align: center;
}

.modal-content h2 {
    color: #e74c3c;
    margin-bottom: 15px;
}

.modal-content p {
    font-size: 16px;
    margin: 10px 0;
}

.modal-content strong {
    color: #333333;
}

.modal-button {
    padding: 10px 20px;
    margin-top: 20px;
    border: none;
    border-radius: 4px;
    background-color: #3498db;
    color: white;
    cursor: pointer;
    font-size: 16px;
}

.modal-button:hover {
    background-color: #2980b9;
}

/* Kakao 로그인 버튼 스타일 */
#kakao-login-btn {
    display: flex;
    justify-content: center; /* 텍스트와 아이콘을 중앙 정렬 */
    align-items: center;
    width: 100%; /* 버튼 너비 */
    padding: 15px; /* 패딩 추가 */
    background-color: #FEE500; /* 카카오톡 노란색 배경 */
    border: none;
    border-radius: 5px;
    margin-top: 20px; /* 버튼 위 여백 */
    cursor: pointer;
    transition: background-color 0.3s ease;
    font-size: 18px; /* 글씨 크기 */
    font-weight: bold;
    position: relative; /* 아이콘과 텍스트 정렬을 위한 상대적 위치 */
}

#kakao-login-btn img {
    width: 50px; /* 아이콘 크기 */
    height: 50px;
    position: absolute; /* 아이콘 위치 고정 */
    left: 10px; /* 왼쪽 여백 설정 */
}

.options {
  text-align: center;
  margin-top: 20px;
}

/* 회원가입과 아이디/비밀번호 찾기 사이에 간격 추가 */
.options a {
  color: #ffffff;
  text-decoration: none;
  font-weight: bold;
  margin-right: 15px; /* 회원가입과 아이디/비밀번호 찾기 사이 간격 */
}

.options a:last-child {
  margin-right: 0; /* 마지막 링크는 오른쪽 여백을 없앱니다 */
}

/* 인풋 필드의 placeholder(자리 표시자) 텍스트 색상 변경 */
.form-control::placeholder {
  color: #ffffff; /* 글자색을 흰색으로 설정 */
  opacity: 1; /* 투명도를 완전히 불투명하게 설정 */
}

/* 인풋 필드의 글자 색상을 흰색으로 변경 */
.form-control {
  color: #ffffff; /* 인풋 필드 안의 글자색을 흰색으로 설정 */
}
